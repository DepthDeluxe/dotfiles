[user]
    name = DepthDeluxe
    email = colin@heinzmann.me
[core]
	editor = vim
[push]
	default = simple

[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
